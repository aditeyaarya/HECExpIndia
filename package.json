{
  "name": "hec-experienceindia",
  "version": "1.0.0",
  "private": true,
  "description": "HEC ExperienceIndia static site generated with Eleventy (Nunjucks templating).",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "start": "npm run serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
